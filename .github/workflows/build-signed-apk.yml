name: CI/CD

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

env:
  FLUTTER_VERSION: "3.38.1"
  ANDROID_VERSION_CODE_OFFSET: "100000"

jobs:
  refresh-lockfile:
    name: Refresh Flutter lockfile
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve and commit lockfile
        run: |
          flutter pub get
          if git diff --quiet -- pubspec.lock; then
            echo "pubspec.lock is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pubspec.lock
          git commit -m "build: refresh Flutter lockfile"
          git push origin "HEAD:${{ github.head_ref }}"

  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Test
        run: flutter test

  build-android:
    name: Build Android flavors
    needs: [analyze, test]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Resolve Android version code
        id: android_version
        run: echo "version_code=$((ANDROID_VERSION_CODE_OFFSET + GITHUB_RUN_NUMBER))" >> "$GITHUB_OUTPUT"

      - name: Build Play release APK
        run: flutter build apk --release --flavor play --build-number "${{ steps.android_version.outputs.version_code }}"

      - name: Build Play release AAB
        run: flutter build appbundle --release --flavor play --build-number "${{ steps.android_version.outputs.version_code }}"

      - name: Build F-Droid release APK
        run: flutter build apk --release --flavor fdroid --build-number "${{ steps.android_version.outputs.version_code }}"

      - name: Upload Play APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: the-universe-decides-apk
          path: build/app/outputs/flutter-apk/app-play-release.apk
          if-no-files-found: error

      - name: Upload Play AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: the-universe-decides-aab
          path: build/app/outputs/bundle/playRelease/app-play-release.aab
          if-no-files-found: error

      - name: Upload F-Droid APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: the-universe-decides-fdroid-apk
          path: build/app/outputs/flutter-apk/app-fdroid-release.apk
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: [build-android]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve version and tag
        id: version
        run: |
          VERSION_NAME=$(grep '^version:' pubspec.yaml | sed -E 's/^version:[[:space:]]*//' | cut -d'+' -f1)
          VERSION_CODE=$((ANDROID_VERSION_CODE_OFFSET + GITHUB_RUN_NUMBER))
          TAG="v${VERSION_NAME}+${VERSION_CODE}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "version_code=$VERSION_CODE" >> "$GITHUB_OUTPUT"

      - name: Download Play APK artifact
        uses: actions/download-artifact@v4
        with:
          name: the-universe-decides-apk
          path: dist

      - name: Download Play AAB artifact
        uses: actions/download-artifact@v4
        with:
          name: the-universe-decides-aab
          path: dist

      - name: Download F-Droid APK artifact
        uses: actions/download-artifact@v4
        with:
          name: the-universe-decides-fdroid-apk
          path: dist

      - name: Rename release artifacts
        run: |
          mv dist/app-play-release.apk "dist/the-universe-decides-${{ steps.version.outputs.tag }}-play.apk"
          mv dist/app-play-release.aab "dist/the-universe-decides-${{ steps.version.outputs.tag }}.aab"
          mv dist/app-fdroid-release.apk "dist/the-universe-decides-${{ steps.version.outputs.tag }}-fdroid.apk"

      - name: Extract changelog for this release
        run: |
          NOTES_FILE="dist/release-notes.md"
          awk '/^## Unreleased/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md > "$NOTES_FILE" || true
          if [ ! -s "$NOTES_FILE" ] || ! grep -q '[^[:space:]]' "$NOTES_FILE"; then
            echo "No CHANGELOG.md '## Unreleased' entry found; falling back to a minimal note." >&2
            echo "See CHANGELOG.md for release notes." > "$NOTES_FILE"
          fi
          echo "notes_file=$NOTES_FILE" >> "$GITHUB_OUTPUT"
        id: changelog

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          body_path: ${{ steps.changelog.outputs.notes_file }}
          files: |
            dist/the-universe-decides-${{ steps.version.outputs.tag }}-play.apk
            dist/the-universe-decides-${{ steps.version.outputs.tag }}-fdroid.apk
            dist/the-universe-decides-${{ steps.version.outputs.tag }}.aab
